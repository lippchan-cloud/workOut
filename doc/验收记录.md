# workOut MVP 验收记录

| 项 | 内容 |
| --- | --- |
| 日期 | 2026-08-18 |
| 分支 | `feat/init-workout-mvp` |
| 方式 | 自动化测试覆盖为主；**未在真实浏览器中完整手点 T00–T13** |

## 结果总表

| 编号 | 用例 | 结果 | 证据 / 缺口 |
| --- | --- | --- | --- |
| T00 | 未登录点 Tab / 保存 | **自动化通过** | `authRedirect.test.tsx`、`AppShell.test.tsx`、`RecordPage.test.tsx` |
| T00b | 注册并登录发 Token | **自动化通过** | `AuthRegisterTest`、`AuthLoginTest` |
| T00c | 错误密码 | **自动化通过** | `AuthLoginTest` 通用文案、无 token |
| T01 | 保存消耗 | **API 通过 / UI 部分** | `DailyRecordCreateTest`；前端保存清空有测；**未在浏览器核对日历绿色** |
| T02 | 保存摄入 | **API 通过 / UI 部分** | 创建 INTAKE + `CalendarPage` 红色 class；**未手测闭环** |
| T03 | 改到昨天保存 | **API 部分** | 服务按 `recordedAt` 落库与按日查询；**无单独「昨天」UI 用例** |
| T04 | 空内容 | **通过** | 后端 400「请填写内容」；前端空内容拦截 |
| T05 | 日历进入当前周今日 | **自动化通过** | `week.test.ts` + Calendar 周条；今日标记「今」 |
| T06 | 切天再切回 | **缺口** | 有选中日状态；**无专门切回今天的断言** |
| T07 | 同日时间正序 | **自动化通过** | `DailyRecordQueryTest` |
| T08 | 导出有数据 | **API 通过** | `CsvExportTest` BOM/表头/中文/文件名；前端点击有 mock |
| T09 | 导出无数据 | **API 通过** | 仅表头 |
| T10 | 资料回显 | **通过** | `ProfileUpsertTest` + `ProfilePage.test.tsx` |
| T11 | 用户隔离 | **API 通过** | `DailyRecordIsolationTest`、`ProfileUpsertTest`、CSV 仅本人 |
| T12 | 无 Token 业务接口 401 | **自动化通过** | `JwtAuthFilterTest`、`CsvExportTest` |
| T13 | CLI 启动浏览器闭环 | **缺口** | 已提供 `./scripts/start.sh`；**本环境未实际 `spring-boot:run` 后用浏览器走通** |

## 启动命令（待人工浏览器验收）

```bash
./scripts/start.sh
# 打开 http://localhost:8080
```

依赖：默认使用 `application.yml` 中的 SQLPub 库与 JWT（私有仓已写入，不设环境变量也可启动）；`WORKOUT_*` 为可选覆盖。**集成测试（`application-test.yml`）同样直连该 SQLPub MySQL**，不再使用 H2；测试写入 `user` / `daily_record` / `profile`，用户名带 UUID 后缀以降低污染。
