# 集成测试直连 SQLPub MySQL（与主配置同一实例），不再使用 H2。
# 测试会向 work_out_* 表写入数据；用例通过 UUID 后缀用户名降低冲突。
# 共享库 baseline-on-migrate + baseline-version:1，启动后由 V2 确保 work_out_* 表存在。
spring:
  datasource:
    url: jdbc:mysql://mysql5.sqlpub.com:3310/inv_doc?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: user_lipp
    password: "pgGJlfNr1pSJbcq1"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 0
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Shanghai
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    baseline-description: Shared inv_doc schema already has other tables

workout:
  jwt:
    secret: test-secret-key-which-is-long-enough-for-hs256-algorithms-0123456789
    expire-days: 7
  admin:
    usernames: ""
